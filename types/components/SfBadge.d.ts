import type { DefineComponent } from 'vue';
export interface SfBadgeProps {
  as?: string;
  tone?: string;
  dot?: boolean;
}
declare const SfBadge: DefineComponent<SfBadgeProps>;
export default SfBadge;
